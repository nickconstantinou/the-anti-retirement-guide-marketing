/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/the-anti-retirement-guide-marketing',
  output: 'export',
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig
